defaults:
  - scope:
      path: ""  # all files
    values:
      layout: "default"

markdown: kramdown  # already the default in Github's Jekyll.
kramdown:
  # I'm doing client-side math processing, just want unambiguous marking of math.
  math_engine: null
