defaults:
  - scope:
      path: ""  # all files
    values:
      layout: "default"

markdown: GFM
kramdown:
  # I'm doing client-side math processing, just want unambiguous marking of math.
  math_engine: null
